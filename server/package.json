{
  "name": "glitch-local-server",
  "version": "1.0.0",
  "description": "Local offline backend for GLITCH Lounge OS — replaces Google Apps Script + Sheets with a local Node server + SQLite file, speaking the exact same action-based protocol.",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "migrate": "node migrate-from-sheets.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  }
}
